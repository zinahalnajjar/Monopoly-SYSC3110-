/**
 * Enum that holds the types of properties (tiles/squares)
 * that can be found on the board
 *
 * @author Tooba
 */
public enum PropertyType {
    RAILROAD,
    UTILITY,
    JAIL,
    PROPERTY,
    GO,
    PASSING_TILE,
}
